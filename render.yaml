services:
  - type: web
    name: flask-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "sh start.sh"
    plan: free
    envVars:
      - key: PORT
        value: 10000
